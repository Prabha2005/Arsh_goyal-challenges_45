class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        nl = len(nums)
        for i in range(nl-1):
            for j in range(i+1,nl):
                if nums[i]+nums[j]==target:
                    return [i,j]
        return []
    
